#include <stdio.h>
#include <stdlib.h>

int main()
{
	char filename[50];
	int bytein;
	int byteoffset;
	char randstring[20]
 printf("Enter the filename to scan: ");
   scanf("%s", &filename);
printf (" Enter the byte count to start at: "); 
scanf ("%d", bytein);
printf("Enter the byte count to offset by: "); 
scanf("%d", byteoffset);
printf("Enter string to change to: ") 
scanf ("%s" ,randstring)
 return 0;
}